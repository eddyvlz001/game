# Database Configuration
DATABASE_URL="postgresql://edubattle_user:password123@localhost:5432/edubattle_db"

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET="edubattle_super_secret_jwt_key_2025_change_in_production"
JWT_EXPIRES_IN="7d"

# CORS Configuration
CORS_ORIGIN="http://localhost:3000,http://localhost:5173"

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_LEVEL=info

# ==========================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ==========================================
# 
# 1. Copia este archivo como .env:
#    cp .env.example .env
#
# 2. Personaliza las variables según tu entorno:
#
# DATABASE_URL:
#   - Cambia 'password123' por una contraseña segura
#   - Si usas un host diferente, actualiza la URL
#   - Ejemplo para producción:
#     DATABASE_URL="postgresql://user:securepass@db.host.com:5432/edubattle_prod"
#
# JWT_SECRET:
#   - CAMBIA ESTE VALOR en producción
#   - Genera una clave secreta segura:
#     node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# CORS_ORIGIN:
#   - Lista de dominios permitidos separados por comas
#   - En producción, especifica los dominios exactos
#
# NODE_ENV:
#   - development: Para desarrollo local
#   - production: Para producción
#   - test: Para pruebas
#
# BCRYPT_ROUNDS:
#   - Número de rondas para hashing (10-12 recomendado)
#   - Mayor número = más seguro pero más lento
#
# ==========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==========================================
#
# Para configurar PostgreSQL:
# 1. Ejecuta: ./scripts/setup-database.sh
# 2. O manualmente:
#    CREATE DATABASE edubattle_db;
#    CREATE USER edubattle_user WITH ENCRYPTED PASSWORD 'password123';
#    GRANT ALL PRIVILEGES ON DATABASE edubattle_db TO edubattle_user;